{
  "name": "marvel-api-server",
  "description": "",
  "version": "1.0.0",
  "author": "Cristian Ramírez",
  "scripts": {
    "clean": "rm -rf build && mkdir build",
    "prestart": "npm run clean && babel ./server -d ./build --copy-files",
    "start": "node ./build/index.js",
    "babel-node": "babel-node --presets=env",
    "server": "nodemon --exec npm run babel-node -- ./server/index.js"
  },
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.1",
    "cors": "^2.8.3",
    "dotenv": "^4.0.0",
    "express": "^4.15.2",
    "marvel-api": "^1.1.0",
    "minimist": "^1.2.0",
    "morgan": "^1.8.1",
    "swagger-node-express": "^2.1.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "nodemon": "^1.14.1"
  }
}
